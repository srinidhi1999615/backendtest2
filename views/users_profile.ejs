<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>User Profile</title>
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <% if (user && user.id === profile_user.id) { %>
                    <!-- Form to update password for the logged-in user -->
                    <form action="/users/update/<%=user._id%>" method="POST">
                        <div class="mb-3">
                            <input type="password" class="form-control" name="password" placeholder="Your new Password" required>
                        </div>
                        <div class="mb-3">
                            <input type="submit" class="btn btn-primary btn-block" value="Update Password">
                        </div>
                    </form>
                <% } else { %>
                    <!-- Display profile info if not the logged-in user -->
                    <div class="mb-3">
                        <p><strong>Name:</strong> <%= profile_user.name %></p>
                    </div>
                    <div class="mb-3">
                        <p><strong>Email:</strong> <%= profile_user.email %></p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-eo8zV/5RxmZFfKNpZbJBSK93xDkJ0hK5IuyYGnTqXfZXAL+0WiK5m9+Xk3zmx3fG" crossorigin="anonymous"></script>
</body>
</html>
